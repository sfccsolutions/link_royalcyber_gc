<isset name="productQuantity" value="${!empty(order.productQuantityTotal) ? order.productQuantityTotal : 0}"
    scope="page" />
<isprint value="${productQuantity + order.giftcertificates.length}" formatter="#" />